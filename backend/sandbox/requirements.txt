# MDP Sandbox Service Dependencies
# All dependencies should already be in mdp-backend base image

# Web framework (already in base)
fastapi>=0.100.0
uvicorn>=0.23.0
pydantic>=2.0.0

# Common libraries (already in base)
numpy>=1.24.0
pandas>=2.0.0
